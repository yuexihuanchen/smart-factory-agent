<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.smartfactory.mapper.SysPermissionMapper">

    <resultMap id="SysPermissionResultMap"
               type="com.smartfactory.entity.SysPermission">

        <id property="id" column="id"/>

        <result property="permissionName"
                column="permission_name"/>

        <result property="permissionCode"
                column="permission_code"/>

        <result property="description"
                column="description"/>

        <result property="createdAt"
                column="created_at"/>

        <result property="updatedAt"
                column="updated_at"/>

    </resultMap>

    <select id="findPermissionsByUserId"
            resultMap="SysPermissionResultMap">

        SELECT DISTINCT
            p.id,
            p.permission_name,
            p.permission_code,
            p.description,
            p.created_at,
            p.updated_at
        FROM sys_permission p
        JOIN sys_role_permission rp
            ON p.id = rp.permission_id
        JOIN sys_user_role ur
            ON rp.role_id = ur.role_id
        WHERE ur.user_id = #{userId}

    </select>

</mapper>